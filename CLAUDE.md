This is the workspace for podtainer - portainer but for podman. You can view stacks (compose files), quadlet units, systemd units, and podman containers. 

The backend is written in go, the frontend is written in vuejs. 

Rely on the user for Smoke/browser testing. You may ensure the frontend/backend builds, but no further testing. 